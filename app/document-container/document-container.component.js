'use strict';

// Register `documentContainer` component, along with its associated controller and template
angular.
  module('documentContainer').
  component('documentContainer', {
    templateUrl: 'document-container/document-container.template.html',
    controller: ['$routeParams',
      function documentContainerController($routeParams, Phone) {
        var self = this;
      
      }
    ]
  });
